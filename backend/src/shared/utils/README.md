# Shared Utilities (`src/shared/utils`)

This directory contains shared helper functions, pagination utilities, and input validators.

## Planned Components
- `pagination.go`: Helper utilities for pagination query parsing and calculations.
- `validator.go`: Input validation helper utilities.
