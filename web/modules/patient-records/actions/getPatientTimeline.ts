import { TimelineEvent, VitalsTrendPoint } from '../types';

export async function getPatientTimelineAction(patientId: string): Promise<TimelineEvent[]> {
  return [];
}

export async function getVitalsHistoryAction(patientId: string): Promise<VitalsTrendPoint[]> {
  return [];
}
